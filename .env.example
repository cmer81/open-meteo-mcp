# Open-Meteo MCP Server Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# Transport Configuration
# =============================================================================

# Transport mode: "stdio" (default) or "http"
MCP_TRANSPORT=http

# HTTP server bind address (default: 127.0.0.1)
# Use 0.0.0.0 to listen on all interfaces (see security note below)
MCP_HOST=127.0.0.1

# HTTP server port (default: 3000)
MCP_PORT=3000

# =============================================================================
# Open-Meteo API Endpoints (all optional, defaults shown)
# =============================================================================

# Main forecast API
# OPEN_METEO_API_URL=https://api.open-meteo.com

# Air quality API
# OPEN_METEO_AIR_QUALITY_API_URL=https://air-quality-api.open-meteo.com

# Marine weather API
# OPEN_METEO_MARINE_API_URL=https://marine-api.open-meteo.com

# Historical/archive data API
# OPEN_METEO_ARCHIVE_API_URL=https://archive-api.open-meteo.com

# Seasonal forecast API
# OPEN_METEO_SEASONAL_API_URL=https://seasonal-api.open-meteo.com

# Ensemble forecast API
# OPEN_METEO_ENSEMBLE_API_URL=https://ensemble-api.open-meteo.com

# Geocoding API
# OPEN_METEO_GEOCODING_API_URL=https://geocoding-api.open-meteo.com

# =============================================================================
# Security Note
# =============================================================================
# When binding to 0.0.0.0 (all interfaces), DNS rebinding protection is
# disabled. For production deployments, consider:
# - Using a reverse proxy (nginx, caddy, etc.)
# - Implementing authentication
# - Restricting access via firewall rules
